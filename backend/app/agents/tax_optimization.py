def future_tax_scope() -> dict[str, list[str]]:
    return {
        "status": ["future"],
        "capabilities": ["80C optimization", "capital gains analysis", "tax harvesting", "tax forecasting"],
    }
